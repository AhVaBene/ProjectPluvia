export class Location {
    constructor(city, address, latitude, longitude) {
      this.city = city;
      this.address = address;
      this.latitude = latitude;
      this.longitude = longitude;
    }
  }